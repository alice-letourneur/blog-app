<div class="jumbotron pages">
  <h2 align="center">Category: <%= @category.name.capitalize %></h2>
  <% if logged_in? and current_user.admin? %>
  <div align="center" class="row">
    <%= link_to "Edit category name", edit_category_path(@category), class: "btn btn-xs btn-primary" %>
  </div>
  <% end %>
  <hr>
  <div class="row">
    <div class="col-md-4 col-md-offset-4 center">
        <%= will_paginate @category_articles %>
    </div>
  </div>
  <div>
    <% if @category_articles.any? %>
      <%= render '/articles/article', obj: @category_articles %>
    <% else %>
      <div align="center" class="row">
        <p>There is no existing article about <%= @category.name.downcase %></p>
        <% if logged_in? %>
          <%= link_to "Create an article about #{@category.name.downcase}", new_article_path, class: "btn btn-xs btn-info" %>
        <% end %>
      </div>
    <% end %>
  </div>
  <div class="row">
    <div class="col-md-4 col-md-offset-4 center">
        <%= will_paginate @category_articles %>
    </div>
  </div>
</div>
